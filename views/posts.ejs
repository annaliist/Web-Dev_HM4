<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Posts</title>
</head>

<body>
    <%- include('./partials/header.ejs') %>

        <div class="postslist">
            <% if (posts.length> 0) { %>
                <% posts.forEach(post=> { %>


                    <div class="posts">

                        <div class="image-box">
                            <a class="singlepost" href="/singlepost/<%= post.id %>">
                                <!-- divMain -->

                                <div class="top">
                                    <!-- divTop -->
                                    <div class="author">
                                        <img src="<%= post.profile %>" alt="">
                                        <div class='<h3 class="comment">'>
                                            <%= post.author %>
                                        </div>
                                    </div>
                                    <div class="date">
                                        <%= post.date %>
                                    </div>
                                </div>

                                <div class="image">
                                    <!-- divImage -->
                                    <div class="image">
                                        <div class='img'> <img src="<%= post.image %>" alt=""> </div>
                                        <div class="comment">
                                            <%= post.body %>
                                        </div>
                                    </div>
                                </div>
                            </a>
                            <div class="bottom">
                                <div class="bottom">
                                    <div id="like">
                                        <a class="like" data-update="<%= post.id %>">Like</a>
                                        <%= post.likes %>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>


                    <% }) %>

                        <% } else { %>
                            <p>You did not post anything...</p>
                            <% } %>
        </div>

        <%- include('./partials/footer.ejs') %>
</body>

</html>

<script>
    const updatereq = document.querySelector('a.like');
    updatereq.addEventListener('click', (e) => {
        const endpoint = `/posts/${updatereq.dataset.update}`;

        fetch(endpoint, {
            method: 'PUT',
        }).then(location.reload())
    });
</script>

<style scoped>
    .like {
        background-color: rgb(238, 237, 237);
        color: #2c3e50;
        font-size: 20px;
        padding: 5px;
        margin: auto;
    }

    .like:hover {
        color: black;
        background-color: rgb(223, 221, 221);
        cursor: pointer;
    }

    .postslist {
        background-color: #efefef;
    }

    .singlepost {
        padding: 0px;
    }

    .image-box {
        background-color: white;
    }

    .posts {
        background-color: white;
        padding-top: 30px;
        max-width: 900px;
        margin: auto;
        padding-bottom: 30px;
    }

    .image-box {
        width: 80%;
        font-family: Georgia;
        font-weight: bold;
        font-size: 20px;
        padding: 5px;
        margin: auto;
        box-shadow: -1px 1px 5px 9px gainsboro;
    }

    .top {
        display: flex;
        height: 40px;
        justify-content: space-between;
        align-items: center;
        margin: 20px;
    }

    .author {
        display: flex;
        justify-content: center;
        align-items: center;
        color: black;
        font-size: 16px;
    }

    .author img {
        height: 30px;
        width: 30px;
        padding-right: 10px;
        border-radius: 50%;
    }

    .date {
        color: black;
        font-size: 16px;
    }

    .comment {
        color: black;
        font-size: 18px;
    }

    .image div {
        padding: 0px 10px 10px 10px;
    }

    .image img {
        display: block;
        padding-bottom: 20px;
        max-width: 100%;
        max-height: 600px;
        width: auto;
        height: auto;
        margin: auto;
    }

    .image-box p {
        margin: 20px;
        margin-top: 30px;
        margin-bottom: 20px;
    }

    .bottom {
        padding: 10px 0px 20px 10px;
    }

    #like {
        padding: 0px;
        margin: 0px;
    }

    /* adjacent sibling selector */
    h3 {
        color: darkgrey;
        font-size: 12px;
        padding-left: 5px;
    }
</style>